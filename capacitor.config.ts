import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.learnbyzantinemusic',
  appName: 'learn-byzantine-music',
  webDir: 'www'
};

export default config;
